<?php

/**
 * Name the function with the version number, as this is how we'll verify the update.
 */
function update_version_x_y_z ($forced = false) {
  // Add any update scripts here.

  // If you have updates that should only be run once, even when forced, check the $forced variable.
  // if ( !$forced ) { // Add one-time-inserts. }
}